# 🏛️ MIZEN Studio — Variables de Entorno (Plantilla)
# Copie a .env.local para desarrollo local

# Entorno de ejecución (development | production)
NODE_ENV=development

# URL base de API (por defecto Same-Origin /api en producción)
VITE_API_BASE_URL=/api
